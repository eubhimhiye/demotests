name: shell-command

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    needs: ["shell-command"]
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: echo string
        run: echo "Helloe world"
      - name: multi string
        run: |
          node -v
          npm -v

    