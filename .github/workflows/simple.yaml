name: shell-command

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        run: actions/checkout@v3
      - name: echo string
        run: echo "Helloe world"
      - name: multi string
        run: |
          node -v
          npm -v

    